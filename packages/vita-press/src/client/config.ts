import type { RuntimeConfig } from 'virtual:vitapress/runtime/config'

/**
 * 定义运行时配置
 *
 * @param config - 运行时配置
 * @returns {RuntimeConfig} 运行时配置
 */
export function defineConfig(config: RuntimeConfig): RuntimeConfig {
  return config
}
